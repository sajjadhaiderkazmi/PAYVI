# Add project specific ProGuard rules here.
# minifyEnabled is off by default (see app/build.gradle.kts); this file is
# kept so it's ready if that's turned on later. Retrofit/Gson/Room/ML Kit
# all publish their own consumer-rules.pro bundled in their AARs, so no
# extra -keep rules should be needed for them.
